Which of the following best describes a subquery in SQL?

Explanation:
A subquery is a query nested inside another query, allowing for more complex data retrieval and manipulation.


In which clauses can a subquery be used?

Explanation:
Subqueries can be used in various parts of a SQL statement, including SELECT, FROM, and WHERE clauses, offering flexibility in query construction.